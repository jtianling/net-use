## 1. Copy Order Consistency

- [x] 1.1 Locate monitoring-screen `C` key copy path and switch it to use the same order-mode output as the current on-screen list.
- [x] 1.2 Ensure copy output follows current display mode (masked/raw) while preserving one-entry-per-line format.

## 2. Verification

- [x] 2.1 Add/update tests that assert clipboard copy order follows current visible order after toggling with `O`.
- [x] 2.2 Run tests and confirm no regressions in monitoring-screen key behaviors.
