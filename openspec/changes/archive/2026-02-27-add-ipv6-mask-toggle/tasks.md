## 1. IPv6 canonical 模型与聚合

- [x] 1.1 调整 `DiscoveredAddress` 的 IPv6 表示为 `/64` canonical 输出格式, 并保持 `Display` 输出一致
- [x] 1.2 在地址聚合逻辑中实现 IPv6 `/64` 掩码计算与去重, 保持 IPv4 `/24` 逻辑不变
- [x] 1.3 增加聚合层测试, 覆盖“同一 `/64` 下多个 IPv6 原始地址不重复输出 canonical”场景

## 2. 监控事件与状态数据结构

- [x] 2.1 扩展监控事件或视图输入模型, 同时传递 IPv4/IPv6 masked 与 raw 所需数据
- [x] 2.2 在监控视图状态中分离维护 `ipv4_masked`/`ipv4_raw` 与 `ipv6_masked`/`ipv6_raw` 集合, 并保证恢复逻辑可复用
- [x] 2.3 更新返回选择页后的数据保留结构, 确保再次进入同目标时 IPv4/IPv6 两类数据都可恢复

## 3. TUI 显示切换与键位

- [x] 3.1 在 `MonitorView` 引入统一地址显示模式状态 (`Masked`/`Raw`), 默认 `Masked`
- [x] 3.2 新增 `S` 快捷键处理逻辑, 同步切换 IPv4/IPv6 两种模式并更新状态提示
- [x] 3.3 更新 IPv4/IPv6 面板渲染与标题, 在不同模式显示对应列表与数量

## 4. 输出, 导出与验证

- [x] 4.1 更新 `--no-tui` 输出路径, 确保 IPv6 新地址按 `/64` 格式打印
- [x] 4.2 更新导出与复制逻辑, 固定输出 IPv4 `/24` + IPv6 `/64` canonical 列表
- [x] 4.3 补充/更新相关测试并运行 `cargo test`, 验证键位切换和输出格式行为
